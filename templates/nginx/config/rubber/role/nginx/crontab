<%
  @read_cmd = 'crontab -l'
  @write_cmd = 'crontab -'
  @additive = ["# rubber-nginx-start", "# rubber-nginx-end"]
%>

# Roll the nginx logs at midnight
0 0 * * * <%= RUBBER_ROOT %>/script/cron-sh rubber util:rotate_logs --directory=/mnt/nginx/logs

