<%
  @path = "#{rubber_env.torquebox_dir}/jboss/bin/standalone.conf"
  @post = "service torquebox restart || true"
%>
## -*- shell-script -*- ######################################################
##                                                                          ##
##  JBoss Bootstrap Script Configuration                                    ##
##                                                                          ##
##############################################################################

#
# This file is optional; it may be removed if not needed.
#

#
# Specify the maximum file descriptor limit, use "max" or "maximum" to use
# the default, as queried by the system.
#
# Defaults to "maximum"
#
#MAX_FD="maximum"

#
# Specify the profiler configuration file to load.
#
# Default is to not load profiler configuration file.
#
#PROFILER=""

#
# Specify the location of the Java home directory.  If set then $JAVA will
# be defined to $JAVA_HOME/bin/java, else $JAVA will be "java".
#
#JAVA_HOME="/opt/java/jdk"

#
# Specify the exact Java VM executable to use.
#
#JAVA=""

if [ "x$JBOSS_MODULES_SYSTEM_PKGS" = "x" ]; then
   JBOSS_MODULES_SYSTEM_PKGS="org.jboss.byteman"
fi

# Uncomment the following line to prevent manipulation of JVM options
# by shell scripts.
#
#PRESERVE_JAVA_OPTS=true

#
# Specify options to pass to the Java VM.
#
if [ "x$JAVA_OPTS" = "x" ]; then
   JAVA_OPTS="-Xms<%= rubber_env.torquebox_min_heap %> -Xmx<%= rubber_env.torquebox_max_heap %> -XX:MaxPermSize=512m -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/mnt/<%= rubber_env.app_name %>-<%= RUBBER_ENV %>/current/tmp -Djava.net.preferIPv4Stack=true"
   JAVA_OPTS="$JAVA_OPTS -Djboss.modules.system.pkgs=$JBOSS_MODULES_SYSTEM_PKGS -Djava.awt.headless=true"
   JAVA_OPTS="$JAVA_OPTS -Djboss.bind.address=<%= rubber_instances[rubber_env.host].internal_ip %>"

   <% if rubber_env.torquebox_use_mod_cluster %>
   JAVA_OPTS="$JAVA_OPTS -Djboss.mod_cluster.proxyList=<%= rubber_instances.for_role('web').collect { |host| "#{host.full_name}:80" }.join(',') %>"
   <% end %>

   JAVA_OPTS="$JAVA_OPTS -Dmsg.jgroups.stack=<%= rubber_env.torquebox_use_multicast ? 'udp' : 'tcp' %>"
   JAVA_OPTS="$JAVA_OPTS -Djruby.home=<%= rubber_env.ruby_path %>"
   JAVA_OPTS="$JAVA_OPTS -Djboss.server.default.config=standalone-ha.xml"
else
   echo "JAVA_OPTS already set in environment; overriding default settings with values: $JAVA_OPTS"
fi

# Sample JPDA settings for remote socket debugging
#JAVA_OPTS="$JAVA_OPTS -agentlib:jdwp=transport=dt_socket,address=8787,server=y,suspend=n"

# Sample JPDA settings for shared memory debugging
#JAVA_OPTS="$JAVA_OPTS -agentlib:jdwp=transport=dt_shmem,server=y,suspend=n,address=jboss"

# Uncomment to not use JBoss Modules lockless mode
#JAVA_OPTS="$JAVA_OPTS -Djboss.modules.lockless=false"

# Uncomment to gather JBoss Modules metrics
#JAVA_OPTS="$JAVA_OPTS -Djboss.modules.metrics=true"


JAVA_OPTS="$JAVA_OPTS -Xss2048k -Djruby.compile.invokedynamic=false $APPEND_JAVA_OPTS"

# Make sure we capture the PID.
LAUNCH_JBOSS_IN_BACKGROUND=true
JBOSS_PIDFILE=<%= rubber_env.torquebox_pid_file %>