<%
  @path = '/etc/munin/plugin-conf.d/rubber'
%>

# munin-node clears out enviroment variables. We need to put our path back in so we can find RVM.
# 'rvm exec' in our munin plugins will take care of setting up the rest of the environment
# needed by Ruby
[*]
env.PATH <%= ENV['PATH'] %>
