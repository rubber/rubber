var1: val1
var2: val2

var3: "#{var1}"
var4: "4 is #{var2}"
var5: "#{var3}"

truevar: true
falsevar: false
truevar_exp: "#{true}"
falsevar_exp: "#{false}"
faketruevar_exp: "#{true} thing"

list1: [lv1, lv2, "#{var1}"]

map1:
  mk1: mv1
  mk2: mv2
  mk3: "#{var2}"

  

roles:
  role1:
    var1: role1val1
    var2: role1val2
    list1: [role1lv1, "#{var2}"]

hosts:
  host1:
    var1: host1val1
    var2: host1val2
    list1: [host1lv1, "#{var2}"]
    